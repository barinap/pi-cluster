apiVersion: v1
kind: Service
metadata:
  name: pg-cluster-lb
spec:
  type: LoadBalancer
  selector:
    cnpg.io/cluster: pg-cluster
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
